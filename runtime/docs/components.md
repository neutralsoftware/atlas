# Components added to the scene object

Components are added to all objects that accept one. These are put in the `components` array of the object, and they are defined as objects with a `type` property that specifies the type of the component, and other properties that define the values for that component.

## Script (`type = "script"`)

A script component is a component that can be added to an object to make it execute a script, which can be used to define custom behavior and logic for the object in the scene. It has the following properties:

* `name`: The name of the class defined in the script code that will be instantiated and attached to the object. This is defined as a string representing the name of the class, and it should match the name of the class defined in the script code for the component to work correctly.
* `variables`: A list of pairs of `name` and `value`, which are meant to be used as variables that can be accessed and modified by the script code. The `name` is a string representing the name of the variable, and the `value` can be any JSON value (e.g., string, number, boolean, array, object, etc.) that represents the initial value of the variable. These variables can be used in the script code to store and manipulate data related to the object in the scene. These values though, have to start with a type definer (e.g., `str<Hello, world!>`, `num<42>`, `bool<true>`, `ref<myObject>`, etc.) to indicate the type of the variable, since the script code will need to know the type of the variable to be able to use it correctly.

## Trait Scripts (`type = "trait_script"`)
A trait script component is simply a script component that is meant to be used as a trait, which means that it can be added to multiple objects in the scene to give them the same behavior and logic defined in the script. It has the same properties as the `Script` component, but with the `type` property set to `trait_script` instead of `script`. Additionally, it has the following properties:

* `traitedType`: The type of the objects that can be traited with this trait script, defined as a string that specifies the type of the objects (e.g., `solid`, `model`, `particle_emitter`, etc.) that can have this trait script added to them. This is used to ensure that the trait script is only added to compatible objects in the scene, and to allow the script code to know what type of object it is attached to for better type checking and error handling.

## Rigidbody (`type = "rigidbody"`)

A rigidbody component is a component that can be added to an object to make it affected by physics. It has the following properties:

* `mass`: The mass of the rigidbody, defined as a single number representing the mass in units. A higher value means that the object will be heavier and will be affected more by gravity and other forces.
* `sendSignal`: The signal that the rigidbody will send when it collides with another object, defined as a string representing the name of the signal. This can be used to trigger events or other actions in the scene when the object collides with something.
* `isSensor`: A boolean value that indicates whether the rigidbody is a sensor or not. If `true`, the rigidbody will not be affected by physics and will not collide with other objects, but it will still send signals when it overlaps with other objects. If `false`, the rigidbody will be affected by physics and will collide with other objects as normal.
* `collider`: An object that defines the collider shape of the rigidbody, which can be used to define the shape and size of the object for collision detection. The collider can be defined as an object with a `type` property that specifies the type of the collider (e.g., `box`, `sphere`, etc.), and other properties that define the values for that collider (e.g., `size`, `radius`, etc.).
* `friction`: The friction of the rigidbody, defined as a single number representing the coefficient of friction (values between 0 and 1). A higher value means that the object will be more resistant to sliding against other objects.
* `tags`: An array of strings representing the tags that are associated with the rigidbody, which can be used to categorize and filter objects in the scene based on their tags. This can be useful for things like collision filtering, where you only want certain objects to collide with each other based on their tags.
* `damping`: The damping of the rigidbody, with two values (`linear` and `angular`) representing the linear and angular damping of the rigidbody, defined as a single number representing the damping in units per second. A higher value means that the object will slow down faster when it is moving or rotating.
* `restituition`: The restitution of the rigidbody, defined as a single number representing the coefficient of restitution (values between 0 and 1). A higher value means that the object will be bouncier and will retain more of its velocity after colliding with another object.
* `motionType`: The motion type of the rigidbody, defined as a string that can be either `static`, `dynamic` or `kinematic`. A `static` rigidbody will not move and will not be affected by physics, but it can still collide with other objects. A `dynamic` rigidbody will be affected by physics and will collide with other objects as normal. A `kinematic` rigidbody will not be affected by physics and will not collide with other objects, but it can still be moved manually by setting its position and rotation in the scene.

## AudioPlayer (`type = "audio_player"`)

An audio player component is a component that can be added to an object to make it play audio. It has the following properties:

* `source`: The source of the audio, which is a reference to an audio file (e.g., `.mp3`, `.wav`, etc.) that defines the sound that will be played by the audio player.
* `position`: The position of the audio player in the scene, defined as an array of three numbers representing the x, y and z coordinates. This can be used to create spatial audio effects, where the sound will be louder when the listener is closer to the audio player and quieter when the listener is farther away.
* `useSpatialization`: A boolean value that indicates whether to use spatialization for the audio or not. If `true`, the audio will be spatialized based on the position of the audio player and the listener in the scene. If `false`, the audio will be played as a non-spatialized sound, which means that it will be heard at the same volume regardless of the position of the audio player and the listener in the scene.
* `volume`: A number that controls the playback volume.
* `loop`: A boolean value that controls whether playback repeats after reaching the end.
* `autoplay`: A boolean value that controls whether playback starts when the component is initialized.

## Joint (`type = "joint"`)

A joint component is a component that can be added to an object to connect it to another object with a joint. It has the following properties:

* `parent`: The GameObject that is the parent of the joint, which is the object that the joint will be attached to. This is defined as a reference to another object in the scene (e.g., by its ID or name).
* `child`: The GameObject that is the child of the joint, which is the object that will be connected to the parent object with the joint. This is defined as a reference to another object in the scene (e.g., by its ID or name).
* `space`: The space in which the joint will operate, defined as a string that can be either `world` or `local`. If `world`, the joint will operate in world space, which means that the position and rotation of the joint will be defined relative to the world origin. If `local`, the joint will operate in local space, which means that the position and rotation of the joint will be defined relative to the parent object.
* `anchor`: The anchor point of the joint, defined as an array of three numbers representing the x, y and z coordinates of the anchor point in the space defined by the `space` property. This is the point where the joint will be attached to the parent and child objects. 
* `breakForce`: The break force of the joint, defined as a single number representing the force in units that will cause the joint to break. A higher value means that the joint will be stronger and will require more force to break.
* `breakTorque`: The break torque of the joint, defined as a single number representing the torque in units that will cause the joint to break. A higher value means that the joint will be stronger and will require more torque to break.

## FixedJoint (`type = "fixed_joint"`)
A fixed joint is a type of joint that can be added to an object to connect it to another object with a fixed joint, which means that the two objects will be rigidly connected and will not be able to move relative to each other. It has the same properties as the `Joint` component, but with the `type` property set to `fixed_joint` instead of `joint`.

## HingeJoint (`type = "hinge_joint"`)
A hinge joint is a type of joint that can be added to an object to connect it to another object with a hinge joint, which means that the two objects will be connected in a way that allows them to rotate around a single axis relative to each other. It has the same properties as the `Joint` component, but with the `type` property set to `hinge_joint` instead of `joint` instead. Additionally, it has the following properties:

* `axis1`: The first axis of the hinge joint, defined as an array of three numbers representing the x, y and z components of the first axis vector. This is the axis around which the child object will rotate relative to the parent object.
* `axis2`: The second axis of the hinge joint, defined as an array of three numbers representing the x, y and z components of the second axis vector. This is the axis that is perpendicular to the first axis and defines the plane in which the child object will rotate relative to the parent object.
* `limits`: Angular limits with three properties:
  * `isEnabled`: A boolean value that indicates whether the angular limits are enabled or not. If `true`, the child object will be limited in its rotation relative to the parent object based on the `min` and `max` properties. If `false`, the child object will be able to rotate freely around the hinge joint without any limits.
  * `minAngle`: The minimum angle of the hinge joint, defined as a single number representing the minimum angle in degrees that the child object can rotate relative to the parent object when the limits are enabled. A higher value means that the child object will be able to rotate more in the negative direction relative to the parent object.
  * `maxAngle`: The maximum angle of the hinge joint, defined as a single number representing the maximum angle in degrees that the child object can rotate relative to the parent object when the limits are enabled. A higher value means that the child object will be able to rotate more in the positive direction relative to the parent object.
* `motor`: Motor settings with three properties:
  * `isEnabled`: A boolean value that indicates whether the motor is enabled or not. If `true`, the motor will apply a torque to the child object to try to reach the target velocity defined by the `targetVelocity` property. If `false`, the motor will not apply any torque to the child object.
  * `targetVelocity`: The target velocity of the motor, defined as a single number representing the target angular velocity in degrees per second that the motor will try to achieve for the child object when the motor is enabled. A higher value means that the motor will try to rotate the child object faster around the hinge joint.
  * `maxTorque`: The maximum torque of the motor, defined as a single number representing the maximum torque in units that the motor can apply to the child object when trying to reach the target velocity. A higher value means that the motor will be able to apply more torque to try to reach the target velocity, which can be useful for overcoming resistance or inertia in the system.

## SpringJoint (`type = "spring_joint"`)
A spring joint is a type of joint that can be added to an object to connect it to another object with a spring joint, which means that the two objects will be connected in a way that allows them to move relative to each other while being pulled together by a spring force. It has the same properties as the `Joint` component, but with the `type` property set to `spring_joint` instead of `joint` instead. Additionally, it has the following properties:

* `anchorB`: The anchor point of the spring joint on the child object, defined as an array of three numbers representing the x, y and z coordinates of the anchor point in the local space of the child object. This is the point where the spring force will be applied to the child object.
* `restLength`: The rest length of the spring joint, defined as a single number representing the length in units that the spring will try to maintain between the parent and child objects. A higher value means that the spring will try to keep the objects farther apart, while a lower value means that the spring will try to keep the objects closer together.
* `useLimits`: A boolean value that indicates whether to use limits for the spring joint or not. If `true`, the spring joint will only apply force when the distance between the parent and child objects is outside of the range defined by the `minLength` and `maxLength` properties. If `false`, the spring joint will apply force based on the difference between the current length and the rest length regardless of any limits.
* `minLength`: The minimum length of the spring joint, defined as a single number representing the minimum length in units that the spring will allow between the parent and child objects when limits are enabled. A higher value means that the spring will not allow the objects to get too close together.
* `maxLength`: The maximum length of the spring joint, defined as a single number representing the maximum length in units that the spring will allow between the parent and child objects when limits are enabled. A higher value means that the spring will not allow the objects to get too far apart.
* `spring`: The spring force of the spring joint with properties:
  * `enabled`: A boolean value that indicates whether the spring force is enabled or not. If `true`, the spring joint will apply a force based on Hooke's law to try to maintain the rest length between the parent and child objects. If `false`, the spring joint will not apply any force based on Hooke's law.
  * `mode`: The mode of the spring force, either `frequencyAndDamping` or `stiffnessAndDamping`. If `frequencyAndDamping`, the spring force will be defined by the `frequency` and `damping` properties. If `stiffnessAndDamping`, the spring force will be defined by the `stiffness` and `damping` properties.
  * `frequencyHz`: The frequency of the spring force when using `frequencyAndDamping` mode, defined as a single number representing the frequency in hertz. A higher value means that the spring will oscillate faster.
  * `damping`: The damping of the spring force, defined as a single number representing the damping in units per second. A higher value means that the spring will lose energy faster and will come to rest more quickly.
  * `stiffness`: The stiffness of the spring force when using `stiffnessAndDamping` mode, defined as a single number representing the stiffness in units per length. A higher value means that the spring will be stronger and will try harder to maintain the rest length between the parent and child objects.
  * `damping`: The damping of the spring force, defined as a single number representing the damping in units per second. A higher value means that the spring will lose energy faster and will come to rest more quickly.

## Vehicle (`type = "vehicle"`)
A vehicle component is a component that can be added to an object to make it behave like a vehicle, which means that it will 
have properties and behavior related to driving and controlling a vehicle. It has the following properties:

* `settings`: With properties:
  * `up`: The up direction of the vehicle, defined as an array of three numbers representing the x, y and z components of the up vector. This is the direction that is considered "up" for the vehicle, which can affect how it interacts with gravity and other forces in the scene.
  * `forward`: The forward direction of the vehicle, defined as an array of three numbers representing the x, y and z components of the forward vector. This is the direction that is considered "forward" for the vehicle, which can affect how it moves and responds to input for controlling the vehicle.
  * `maxPitchRollAngleDeg`: A single number representing the maximum pitch and roll angle in degrees that the vehicle can achieve when turning or going over uneven terrain. A higher value means that the vehicle will be able to tilt more when turning or going over bumps, while a lower value means that the vehicle will be more stable and will not tilt as much.
  * `wheels`: An array of wheel objects each with properties:
    * `position`: The position of the wheel relative to the vehicle, defined as an array of three numbers representing the x, y and z coordinates of the wheel in the local space of the vehicle. This is the point where the wheel will be attached to the vehicle and will interact with the ground.
    * `enableSuspensionForcePoint`: A boolean value that indicates whether to enable the suspension force point for the wheel or not. If `true`, the wheel will have a suspension force point that can be used to apply forces to the vehicle based on the compression of the suspension. If `false`, the wheel will not have a suspension force point and will not apply any forces based on suspension compression.
    * `suspensionForcePoint`: The position of the suspension force point for the wheel, defined as an array of three numbers representing the x, y and z coordinates of the suspension force point in the local space of the vehicle. This is the point where the suspension force will be applied to the vehicle when the suspension is compressed.
    * `suspensionDirection`: The direction of the suspension for the wheel, defined as an array of three numbers representing the x, y and z components of the suspension direction vector. This is the direction in which the suspension will apply forces to the vehicle when it is compressed.
    * `steeringAxis`: The axis around which the wheel will steer, defined as an array of three numbers representing the x, y and z components of the steering axis vector. This is the axis that the wheel will rotate around when steering input is applied to the vehicle.
    * `wheelUp`: The up direction of the wheel, defined as an array of three numbers representing the x, y and z components of the wheel up vector. This is the direction that is considered "up" for the wheel, which can affect how it interacts with the ground and other forces in the scene.
    * `wheelForward`: The forward direction of the wheel, defined as an array of three numbers representing the x, y and z components of the wheel forward vector. This is the direction that is considered "forward" for the wheel, which can affect how it moves and responds to input for controlling the vehicle.
    * `suspensionMinLength`: The minimum length of the suspension for the wheel, defined as a single number representing the minimum length in units that the suspension can be compressed. A higher value means that the suspension will not be able to compress as much, while a lower value means that the suspension will be able to compress more.
    * `suspensionMaxLength`: The maximum length of the suspension for the wheel, defined as a single number representing the maximum length in units that the suspension can be extended. A higher value means that the suspension will be able to extend more, while a lower value means that the suspension will not be able to extend as much.
    * `suspensionPreloadLength`: The preload length of the suspension for the wheel, defined as a single number representing the preload length in units that the suspension will be compressed by default. A higher value means that the suspension will be compressed more by default, while a lower value means that the suspension will be compressed less by default.
    * `suspensionFrequencyHz`: The frequency of the suspension for the wheel, defined as a single number representing the frequency in hertz. A higher value means that the suspension will oscillate faster, while a lower value means that the suspension will oscillate slower.
    * `suspensionDampingRatio`: The damping ratio of the suspension for the wheel, defined as a single number representing the damping ratio (values between 0 and 1). A higher value means that the suspension will lose energy faster and will come to rest more quickly, while a lower value means that the suspension will lose energy slower and will oscillate for longer.
    * `radius`: The radius of the wheel, defined as a single number representing the radius in units. A higher value means that the wheel will be larger, while a lower value means that the wheel will be smaller.
    * `width`: The width of the wheel, defined as a single number representing the width in units. A higher value means that the wheel will be wider, while a lower value means that the wheel will be narrower.
    * `inertia`: The inertia of the wheel, defined as a single number representing the inertia in units. A higher value means that the wheel will be more resistant to changes in its rotational velocity, while a lower value means that the wheel will be less resistant to changes in its rotational velocity.
    * `angularDamping`: The angular damping of the wheel, defined as a single number representing the angular damping in units per second. A higher value means that the wheel will lose angular velocity faster and will come to rest more quickly, while a lower value means that the wheel will lose angular velocity slower and will oscillate for longer.
    * `maxSteerAngleDeg`: The maximum steer angle of the wheel, defined as a single number representing the maximum steer angle in degrees that the wheel can achieve when steering input is applied to the vehicle. A higher value means that the wheel will be able to steer more, while a lower value means that the wheel will be able to steer less.
    * `maxBreakTorque`: The maximum brake torque of the wheel, defined as a single number representing the maximum brake torque in units that can be applied to the wheel when braking input is applied to the vehicle. A higher value means that the wheel will be able to brake harder, while a lower value means that the wheel will be able to brake less.
    * `maxHandBreakTorque`: The maximum handbrake torque of the wheel, defined as a single number representing the maximum handbrake torque in units that can be applied to the wheel when handbrake input is applied to the vehicle. A higher value means that the wheel will be able to brake harder with the handbrake, while a lower value means that the wheel will be able to brake less with the handbrake.
  * `controller`: With properties:
    * `engine`: With properties:
      * `maxTorque`: The maximum engine torque, defined as a single number representing the maximum torque in units that can be applied to the wheels by the engine. A higher value means that the engine will be able to apply more torque to the wheels, while a lower value means that the engine will be able to apply less torque to the wheels.
      * `minRPM`: The minimum engine RPM, defined as a single number representing the minimum revolutions per minute (RPM) that the engine can achieve. A higher value means that the engine will be able to rev higher, while a lower value means that the engine will not be able to rev as high.
      * `maxRPM`: The maximum engine RPM, defined as a single number representing the maximum revolutions per minute (RPM) that the engine can achieve. A higher value means that the engine will be able to rev higher, while a lower value means that the engine will not be able to rev as high.
      * `inertia`: The inertia of the engine, defined as a single number representing the inertia in units. A higher value means that the engine will be more resistant to changes in its RPM, while a lower value means that the engine will be less resistant to changes in its RPM.
      * `angularDamping`: The angular damping of the engine, defined as a single number representing the angular damping in units per second. A higher value means that the engine will lose RPM faster and will come to rest more quickly, while a lower value means that the engine will lose RPM slower and will oscillate for longer.
    * `transmission`: With properties:
      * `type`: The type of the transmission, defined as a string that can be either `automatic` or `manual`. If `automatic`, the transmission will automatically shift gears based on the RPM and other factors. If `manual`, the transmission will require manual input to shift gears.
      * `gearRatios`: An array of numbers representing the gear ratios for each gear in the transmission. The length of the array determines the number of gears in the transmission, and each value represents the gear ratio for that gear. A higher value means that the gear will provide more torque to the wheels, while a lower value means that the gear will provide less torque to the wheels.
      * `reverseGearRatio`: The gear ratio for the reverse gear, defined as a single number representing the gear ratio for the reverse gear. A higher value means that the reverse gear will provide more torque to the wheels, while a lower value means that the reverse gear will provide less torque to the wheels.
      * `switchTime`: The time it takes to switch gears, defined as a single number representing the time in seconds that it takes to switch from one gear to another. A higher value means that it will take longer to switch gears, while a lower value means that it will take less time to switch gears.
      * `clutchReleaseTime`: The time it takes to release the clutch when switching gears, defined as a single number representing the time in seconds that it takes to release the clutch when switching from one gear to another. A higher value means that it will take longer to release the clutch, while a lower value means that it will take less time to release the clutch.
      * `switchLatency`: The latency of switching gears, defined as a single number representing the latency in seconds between the input to switch gears and the actual switch of gears. A higher value means that there will be more delay between the input and the gear switch, while a lower value means that there will be less delay between the input and the gear switch.
      * `shiftUpRPM`: The RPM at which the transmission will shift up to the next gear when in automatic mode, defined as a single number representing the RPM in revolutions per minute. A higher value means that the transmission will shift up at a higher RPM, while a lower value means that the transmission will shift up at a lower RPM.
      * `shiftDownRPM`: The RPM at which the transmission will shift down to the previous gear when in automatic mode, defined as a single number representing the RPM in revolutions per minute. A higher value means that the transmission will shift down at a higher RPM, while a lower value means that the transmission will shift down at a lower RPM.
      * `clutchStrength`: The strength of the clutch, defined as a single number representing the strength in units. A higher value means that the clutch will be stronger and will be able to transmit more torque without slipping, while a lower value means that the clutch will be weaker and will slip more easily under high torque.
    * `differentials`: A list of items with properties:
      * `leftWheel`: The index of the left wheel that is connected to the differential, defined as a single number representing the index of the left wheel in the `wheels` array. This is the wheel that will receive torque from the differential on the left side.
      * `rightWheel`: The index of the right wheel that is connected to the differential, defined as a single number representing the index of the right wheel in the `wheels` array. This is the wheel that will receive torque from the differential on the right side.
      * `differentialRatio`: The differential ratio, defined as a single number representing the ratio of torque distribution between the left and right wheels. A higher value means that more torque will be sent to the left wheel, while a lower value means that more torque will be sent to the right wheel.
      * `leftRightSplit`: The left-right split of the differential, defined as a single number representing the percentage of torque that will be sent to the left wheel (values between 0 and 100). A higher value means that more torque will be sent to the left wheel, while a lower value means that more torque will be sent to the right wheel.
      * `limitedSlipRatio`: The limited slip ratio of the differential, defined as a single number representing the maximum difference in speed between the left and right wheels before the limited slip mechanism engages. A higher value means that the limited slip mechanism will engage at a higher speed difference, while a lower value means that the limited slip mechanism will engage at a lower speed difference.
      * `engineTorqueRatio`: The engine torque ratio of the differential, defined as a single number representing the percentage of engine torque that will be sent to the wheels through the differential (values between 0 and 100). A higher value means that more engine torque will be sent to the wheels, while a lower value means that less engine torque will be sent to the wheels.
    * `differentialLimitedSlipRatio`: The limited slip ratio for the entire vehicle, defined as a single number representing the maximum difference in speed between the left and right wheels before the limited slip mechanism engages for the entire vehicle. A higher value means that the limited slip mechanism will engage at a higher speed difference, while a lower value means that the limited slip mechanism will engage at a lower speed difference for the entire vehicle.
  * `maxSlopeAngleDeg`: The maximum slope angle in degrees that the vehicle can climb, defined as a single number representing the maximum slope angle in degrees. A higher value means that the vehicle will be able to climb steeper slopes, while a lower value means that the vehicle will not be able to climb as steep slopes.
