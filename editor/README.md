# Atlas Editor

Electron-based editor for Atlas Engine. It provides a user-friendly interface for managing projects, configuring settings, and running the engine.

## Building
To build the editor for development, run:

```bash
bun run dist:debug
```

To build the editor for release, run:

```bash
bun run release
```